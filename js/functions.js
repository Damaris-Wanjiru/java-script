/*function sayHello() {
    console.log('hello');
}
sayHello();
{
    function fizzBuzz(n) {
        for (i = 0; i <= n; i++) {
            console.log(i);
        }

    } fizzBuzz(5)

}
{
    const numbers = [56, 78, 89, 90]
    for (let i in numbers) {
        console.log(number[i])
    }
    for (let number of numbers) {
        console.log(number)
    }
    // function to get the sum of numbers in an array
    function getSum(arr) {
        sum = sum + i
        return sum;
    }
    console.log(getSum(numbers))
    //function to get for of
    function getSum(arr) {
        sum = 0;
        for (let i of arr) {
            sum + i
        } return sum
    } console.log(numbers)

    //function to get for in
    function getSum(arr) {
        for (let i in arr){
            sum =sum + numbers[i]
        }return sum;
}console.log(numbers)
}
*/